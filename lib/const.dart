void Const(){
  /**
   * Variable's declared with const keyword can be reassigned 
   * and even the memory space can't be relocated.
   * Const can't reside inside a class 
   * 
   * NB: ONCE WE HAVE DEFINED A CONST, 
   * EVEN IF THE VALUE IS NOT USED, THE MEMORY SPACE IS ALREADY OCCUPIED.
   */
  const number = 23;
}